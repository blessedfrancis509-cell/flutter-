import 'package:flutter/material.dart';

/// Centralised colour palette for the ZenCash app.
class AppColors {
  AppColors._();

  // ── Brand purples ───────────────────────────────────────────────
  static const Color primaryPurple = Color(0xFF6C3AED);
  static const Color primaryPurpleDark = Color(0xFF4C1D95);
  static const Color primaryPurpleDeep = Color(0xFF1E0A3C);
  static const Color primaryPurpleLight = Color(0xFF8B5CF6);
  static const Color accentViolet = Color(0xFFA78BFA);

  // ── Greens ──────────────────────────────────────────────────────
  static const Color sendGreen = Color(0xFF22C55E);
  static const Color sendGreenDark = Color(0xFF16A34A);

  // ── Surfaces ────────────────────────────────────────────────────
  static const Color scaffoldBackground = Color(0xFFF7F7FB);
  static const Color cardSurface = Color(0xFFFFFFFF);
  static const Color cardSurfaceAlt = Color(0xFFF4F3F8);
  static const Color dividerColor = Color(0xFFEDEDF2);

  // ── Text ────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFF9CA3AF);
  static const Color textOnDark = Color(0xFFFFFFFF);
  static const Color textOnDarkMuted = Color(0xB3FFFFFF);

  // ── Icon tint backgrounds (service rows) ────────────────────────
  static const Color zenithRed = Color(0xFFEF4444);
  static const Color cardsPurple = Color(0xFF7C3AED);
  static const Color investGreen = Color(0xFF22C55E);
  static const Color businessNavy = Color(0xFF1E293B);

  // ── Bottom navigation ───────────────────────────────────────────
  static const Color navActive = Color(0xFF6C3AED);
  static const Color navInactive = Color(0xFFB0B0C0);
  static const Color navBackground = Color(0xFFFFFFFF);

  // ── Shadows ─────────────────────────────────────────────────────
  static const Color shadowSoft = Color(0x0D000000);
  static const Color shadowMedium = Color(0x14000000);
}
